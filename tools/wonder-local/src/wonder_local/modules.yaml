local_models:
  path: wonder_local.model.local_models.local_models
  object_method: true

load_model:
  path: wonder_local.model.load.load_model
  object_method: true

generate:
  path: wonder_local.model.generate.generate
  object_method: true
  requires:
    - load_model
    - estimate

train:
  path: wonder_local.model.train.train
  object_method: true
  requires:
    - load_model

estimate:
  path: wonder_local.util.estimation.generate_estimated
  object_method: false
